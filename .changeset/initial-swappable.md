---
"@ui-registry/swappable": minor
---

Initial release: `createSwappable` two-step factory with typed slots, per-variant sub-components, `Match`, `useVariant`, `useVariantProps` and `defineConfig`.
